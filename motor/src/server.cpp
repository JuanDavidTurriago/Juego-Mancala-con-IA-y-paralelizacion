#include "alphabeta.h"
#include "mcts.h"

#include <iostream>

int main() {
  // Placeholder server. In fases siguientes se reemplaza por un servidor HTTP real que exponga /move.
  std::cerr << "mancala_server: TODO implementar servidor HTTP (/move, /healthz)\n";
  std::cerr << "This binary currently does nothing.\n";
  return 0;
}

